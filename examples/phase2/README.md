# Phase 2 Examples

This directory demonstrates graph-native Phase 2 capabilities:

- notifications (`examples/phase2/notifications`)
- API resource specs (`examples/phase2/api`)
- generated docs samples (`examples/phase2/docs`)
- deep test generation notes (`examples/phase2/tests`)

All examples are designed to compile through the semantic compiler and emit graph-backed projections.
